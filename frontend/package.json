{
  "name": "vulnclaw-web-ui",
  "version": "0.3.8",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "node -e \"const [maj,min]=process.versions.node.split('.').map(Number);if(maj<22||(maj===22&&min<18)){console.error('lint requires Node >=22.18 (oxlint needs native TS type stripping to load the anti-slop plugin); found '+process.versions.node);process.exit(1);}\" && oxlint",
    "preview": "vite preview"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.59.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@oxlint/plugins": "^1.78.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "oxlint": "^1.78.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
